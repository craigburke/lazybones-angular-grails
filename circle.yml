machine:
  java:
    version: oraclejdk8
    
test:
  override:
    - ./gradlew buildTestApps:
    - ./bin/test.sh:
        parallel: true
        
deployment:
  master:
    branch: master
    commands:
      - ./bin/deploy.sh